<%- include("partials/header") %>

<% movies.forEach(function(movie){ %>
	<div>
		<h2>
			<%= movie.title %>
		</h2>
		<img src="<%= movie.image %>" >
		<p>
			<%= movie.body %>
		</p>
	</div>
<% }) %>

<%- include("partials/footer") %>